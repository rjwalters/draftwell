Bug is already fixed on main by commit 27b8343 (Add mx-auto to container classes for centering). Both container elements in Layout.tsx (lines 12 and 103) already include mx-auto alongside container, which is the exact fix described in the issue. No Playwright infrastructure exists in the project, so the screenshot verification criterion cannot be checked, but the CSS fix is confirmed correct.
